diff --git a/static/script.js b/static/script.js
index 0000000..1111111 100644
--- a/static/script.js
+++ b/static/script.js
@@ -1148,8 +1148,15 @@ function rebuildProblemDropdowns(data) {
       container.querySelectorAll(".checkbox-item").forEach(r => {
         const visible = (!q) || r.dataset.short.includes(q) || r.dataset.full.includes(q);
         r.style.display = visible ? "" : "none";
       });
-      if (input.value.endsWith(", ")) addCustomTokensFromInput(kind);
-      list.classList.remove("hidden");
+      if (input.value.endsWith(", ")) {
+        addCustomTokensFromInput(kind);
+        // reopen and reset filter after typing comma-space
+        container.querySelectorAll(".checkbox-item").forEach(r => r.style.display = "");
+        list.classList.remove("hidden");
+      } else {
+        // show filtered items during typing
+        list.classList.remove("hidden");
+      }
     });